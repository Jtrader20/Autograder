export default {
    db: {
        connection: {
            host: '127.0.0.1',
            user: 'root',
            password: 'root',
            database: 'grader',
            connectionTimeout: 60000
        }
    }
}